#include <cstdint>
#include <iostream>
#include <mutex>
#include <vector>

int main(int argc, char *argv[]) {
  int_fast16_t limit = 10;
  for (int j = 0; j < limit; j++) {
    std::cout << limit << std::endl;
  }

  std::vector<int> a;
}
